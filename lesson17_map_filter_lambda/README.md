```
-- Простые задания

1. Создай лямбда-функцию, которая возвращает квадрат числа.

2. Создай лямбду, которая возвращает сумму двух аргументов.

3. Напиши лямбду, которая возвращает `True`, если число чётное.

4. Используй `map()` и лямбду, чтобы возвести все элементы списка `[1, 2, 3, 4, 5]` в квадрат.

5. С помощью `filter()` и лямбды оставь в списке `[1, 10, 15, 3, 7]` только числа больше 5.

6. Создай лямбду, которая возвращает длину строки.

7. Создай лямбду, которая возвращает последнюю букву слова.

8. Отсортируй список строк по длине с помощью `sorted()` и лямбды.

--- Средний уровень

9. С помощью `reduce()` и лямбды найди произведение всех чисел в списке `[1, 2, 3, 4]`.

10. С помощью лямбды посчитай, является ли строка палиндромом.

11. Используй `map()` с лямбдой, чтобы прибавить 10 к каждому элементу списка `[1, 2, 3]`.

12. Отсортируй список кортежей по второму элементу:
   data = [(1, 5), (3, 2), (2, 8)]
   
13. С помощью лямбды составь функцию, которая возвращает "adult", если возраст больше или равен 18, иначе "child".

14. Отфильтруй строки, начинающиеся с буквы "a":
    ords = ["apple", "banana", "apricot", "cherry"]
    
15. Создай список только с длинами строк из списка слов, используя `map()` и лямбду.

--- Продвинутый уровень

16. Преобразуй список чисел в строку с помощью `map()` и лямбды:
    [1, 2, 3] → ["1", "2", "3"]

17. С помощью лямбды и `sorted()` отсортируй список слов по последней букве.

18. Создай лямбда-функцию, возвращающую максимум из трёх чисел.

19. Сгруппируй строки по их первой букве с использованием лямбды и 

20. Составь список функций (лямбд), каждая из которых возводит число в степень от 0 до 5.
    Пример результата:
        funcs = [lambda x, n=n: x**n for n in range(6)]
```